kind = "web"
previewPath = "/"
title = "Fachat"
version = "1.0.0"
id = "artifacts/fachat"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19750

[services.development]
run = "pnpm --filter @workspace/fachat run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fachat", "run", "build" ]
publicDir = "artifacts/fachat/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19750"
BASE_PATH = "/"
